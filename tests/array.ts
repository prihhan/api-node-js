// create an array of numbers
let users: number[] = [12,34,56,7,8,99];
for (let i = 0; i < users.length; i++) {
    // console.log('element at index ' + i + ' is ' + users[i]);
    console.log('current iteration number: ' + i);
    console.log('current array value ' + users[i]);
    if (users[i] > 30) {
        console.log('print users more than 30: ' + users[i]);
    }
}






